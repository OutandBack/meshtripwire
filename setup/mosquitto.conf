# Minimal Mosquitto config for the Docker stack.
# Sensors on the LAN publish to port 1883; lock this down with
# password_file / TLS before exposing beyond a trusted network.
listener 1883
allow_anonymous true
